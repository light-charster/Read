<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/yuanshen"
        android:label="@string/app_name"
        android:roundIcon="@drawable/yuanshen"
        android:supportsRtl="true"
        android:theme="@style/Theme.Read"
        tools:targetApi="31">
        <activity
            android:name=".Advertisement.advertisementActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Web.SelectActivity"
            android:exported="false"
            android:label="@string/title_activity_select"
            android:theme="@style/Theme.Read" />
        <activity
            android:name=".Web.WebActivity"
            android:exported="false" />
        <activity
            android:name=".Read.ReadActivity"
            android:exported="false" />
        <activity
            android:name=".Main.MainActivity"
            android:exported="false">
        </activity>
    </application>

</manifest>